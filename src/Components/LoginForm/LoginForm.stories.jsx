import React from 'react';

import LoginForm from './LoginForm';

export default {
  component: LoginForm,
  title: 'Form',
};

const Template = args => <LoginForm />;

export const MaterialLoginForm = Template.bind({});
