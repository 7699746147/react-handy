import React from 'react';

import MultilevelForm from './MultilevelForm';

export default {
  component: MultilevelForm,
  title: 'Form',
};

const Template = args => <MultilevelForm />;

export const FormArraySample = Template.bind({});
